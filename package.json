{
  "name": "schedule-calendar",
  "version": "1.0.0",
  "description": "Schedule Calendar App with JWT Auth and SQLite",
  "type": "module",
  "main": "server/server.js",
  "scripts": {
    "start": "node --no-warnings server/server.js",
    "dev": "nodemon --exec \"node --no-warnings\" server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
